### Android Guidelines

- Validate lifecycle handling (`onCreate`, `onResume`, etc.).
- Ensure proper use of ViewModel and LiveData.
- Avoid memory leaks (context misuse, static references).
- Validate Gradle module structure.
