### Java Guidelines

- Ensure null-safety and avoid `NullPointerException`.
- Validate Gradle dependency scopes (`implementation`, `api`, `testImplementation`).
- Review exception handling and logging.
- Check concurrency primitives (`synchronized`, `ExecutorService`, `CompletableFuture`).
- Ensure proper package structure and separation of concerns.
