### PowerShell Guidelines

- Validate parameter attributes and input types.
- Use `Write-Verbose`, `Write-Error`, and structured output.
- Avoid storing secrets in scripts.
- Ensure proper pipeline usage.
- Validate error handling (`-ErrorAction`, `try/catch`).
